"""Vector compression codecs.

This subpackage will host ``BaseCodec`` (ABC) and concrete
implementations: ``SQ8Codec`` in v0.1, ``PQCodec`` in v0.2,
``RaBitQCodec`` in v0.3.

Empty in the scaffold commit; populated in subsequent refactors.
"""
