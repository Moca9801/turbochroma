# Supply-chain review for pull requests (known vulnerabilities in new deps).
# https://github.com/actions/dependency-review-action
name: dependency-review
on: [pull_request]

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: "Dependency review"
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: moderate
